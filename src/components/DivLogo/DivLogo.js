const DivLogo = (props) => {
    return  (
                <div className={props.divprops}>
                    <i className={props.logoprops}></i>
                </div>
            )
}
export default DivLogo